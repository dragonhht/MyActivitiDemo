server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://my.dragon.com:3306/actstudy?useUnicode=true&characterEncoding=UTF-8
    username: huang
    password: 123
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        format_sql: true
        use_sql_comments: true
        temp:
          use_jdbc_metadata_defaults: false
  activiti:
    database-schema: actstudy
    database-schema-update: true
    history-level: full
